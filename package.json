{
  "scripts": {
    "test:integration": "jest --config jest.config.integration.js",
    "test:mutate": "npm run test:mutate --prefix app && npm run test:mutate --prefix functions",
    "test:mutate:user-web": "npm run test:mutate --prefix app",
    "test:mutate:functions": "npm run test:mutate --prefix functions",
    "crap": "node scripts/crap-analysis.mjs"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "firebase": "^12.9.0",
    "firebase-admin": "^13.6.1",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "ts-morph": "^27.0.2",
    "typescript": "^5.9.3"
  }
}
